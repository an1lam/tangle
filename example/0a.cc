#include<iostream>
using std::cerr;
using std::cout;

int main(int argc, char* argv[]) {
  // The following statements are intended to be hooks for us to actually do something in a
  // later layer.

  // Print Something
  // End Print Something
}
